{
  "name": "browser-control-daemon",
  "version": "1.1.1",
  "description": "Local daemon bridging AI agents with Chrome browser automation. Provides HTTP + WebSocket API for browser control.",
  "main": "daemon.js",
  "bin": {
    "browser-control": "./browser-control.js"
  },
  "scripts": {
    "start": "node daemon.js",
    "cli": "node browser-control.js",
    "typecheck": "node --check daemon.js && node --check browser-control.js && node --check protocol.js && node --check artifact-store.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "browser",
    "automation",
    "chrome",
    "agent",
    "ai"
  ],
  "license": "MIT"
}
